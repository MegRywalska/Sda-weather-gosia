package com.sda.weather;

import com.fasterxml.jackson.databind.ObjectMapper;

public class WeatherApplication {
    public static void main(String[] args) {
        // todo create UserInterface and other objects
       // UserInterface userInterface = new UserInterface(new LocationController(new ObjectMapper(), new LocationService(new LocationRepository(), new ObjectMapper());
//        userInterface.open();
//        userInterface.runApplication();
    }
}
